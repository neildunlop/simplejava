---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker pull jeanblanchard/java
    tag: '8'

inputs:
  - name: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=false", "package"]
